## Configure the system first (FOR USER)
Please set populate the configuration data first before using the app.
* Set Connection Type
* Set Companies
* Set Area (cities, localities, sub localities)
* Set the Packages 
* Set the Media Boxes
